{
  "name": "supertokens-react-native",
  "version": "1.2.1",
  "description": "React Native SDK for SuperTokens",
  "main": "index.js",
  "scripts": {
    "build-check": "cd lib && npx tsc -p tsconfig.json --skipLibCheck --noEmit",
    "build": "cd lib && npx tsc -p tsconfig.json --skipLibCheck && cd ../",
    "pretty": "cd lib && npx prettier --write --config .prettierrc \"ts/**/*.ts\" \"build/**/*.js\" \"../TestingApp/test/**/*.js\"",
    "build-pretty": "npm run build && npm run pretty",
    "pretty-check": "cd lib && npx prettier --check --config .prettierrc \"ts/**/*.ts\" \"build/**/*.js\" \"../TestingApp/test/**/*.js\"",
    "set-up-hooks": "cp hooks/pre-commit.sh .git/hooks/pre-commit && chmod +x .git/hooks/pre-commit"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/supertokens/supertokens-react-native.git"
  },
  "keywords": [
    "react-native",
    "authentication",
    "authorisation",
    "supertokens",
    "ios",
    "android"
  ],
  "author": "rishabhpoddar",
  "license": "Apache 2.0",
  "bugs": {
    "url": "https://github.com/supertokens/supertokens-react-native/issues"
  },
  "homepage": "https://github.com/supertokens/supertokens-react-native#readme",
  "peerDependencies": {
    "axios": "*",
    "react-native": ">=0.61.5 <1.0.x",
    "@react-native-async-storage/async-storage": "^1.12.1"
  },
  "devDependencies": {
    "@react-native-async-storage/async-storage": "^1.12.1",
    "@types/base-64": "^1.0.0",
    "@types/react-native": "0.62.0",
    "axios": "*",
    "prettier": "1.18.2",
    "typescript": "3.5.2"
  },
  "dependencies": {
    "base-64": "^1.0.0"
  }
}
